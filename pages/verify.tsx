import { VerificationSetup } from "@/components/verification/verification-setup";
import React from "react";

const Verify = () => {
  return (
    <section>
      <VerificationSetup />
    </section>
  );
};

export default Verify;
